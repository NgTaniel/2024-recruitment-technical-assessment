Please detail the instructions to view your solution here:

The submission is created using vite and react, the instructions are as follows:

1. Fork the repository and change directory using 'cd uni-task'

2. Input in the terminal 'npm run dev', then hover over the link (should look something like http://localhost:5173/) and 'ctrl + click'

3. The frontend page should appear in a new tab
